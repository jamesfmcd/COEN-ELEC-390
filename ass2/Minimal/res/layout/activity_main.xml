<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="ca.concordia.sensortag.minimal.MainActivity$PlaceholderFragment" >

    <TextView
        android:id="@+id/helloworld"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/hello_world"
        android:textSize="32sp" />

  
    <TextView
        android:id="@+id/the_humidity_is"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/the_humidity_is"
        android:layout_marginTop="95dp"
        android:text="@string/the_humidity_is"
        android:textAppearance="?android:attr/textAppearanceMedium" />
    <!--  This is a comment -->

    <TextView
        android:id="@+id/humidityid"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/the_humidity_is"
        android:layout_alignBottom="@+id/the_humidity_is"
        android:layout_marginLeft="27dp"
        android:layout_toRightOf="@+id/the_humidity_is"
        android:text="@string/humidity"
        android:textAppearance="?android:attr/textAppearanceMedium" />

    <ToggleButton
        android:id="@+id/humidextoggleButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="36dp"
        android:text="@string/humidex_button" />

</RelativeLayout>
